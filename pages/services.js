import React from 'react'
import Layout from '../components/Layout';

const services = (props) => {
    return (
      <Layout>
        <main>
            <h1 className="text-left md:text-center text-2xl">Nos Services</h1>
        </main>
      </Layout>
    );
}

export default services
